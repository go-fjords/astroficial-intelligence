/**
 * This module contains constants about the game as seen by the frontend.
 * Constants describe things such as sizes, colors, and other things that
 * are used in the frontend.
 */

// CONSTANTS describing central hexagon properties
export const HEXAGON_SIZE = .5;
export const HEXAGON_WIDTH = Math.sqrt(3) * HEXAGON_SIZE;
export const HEXAGON_HEIGHT = HEXAGON_SIZE * 2;